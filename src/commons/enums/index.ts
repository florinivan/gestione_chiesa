/**
 * @enum
 * This is the filter for the prematch
 */
export enum TimeFilter {
  All = 0,
  Today = 1,
  Tomorrow = 4,
  DayAfterTomorrow = 3
}

/**
 * @enum
 * Widget order identifier
 */
export enum WidgetName {
  Strikers = 'strikers',
  HotBets = 'hot-bets',
  ChosenForYou = 'for-you',
  SisalNews = 'sisal-news',
  OurApp = 'our-app',
  HeroBanner = 'hero-banner'
}
