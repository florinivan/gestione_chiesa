/**
 * @todo REMOVE AFTER TESTS
 */
export function getImagePath(image: string) {
  // if (process.env.REACT_APP_DEPLOY_MODE === 'aem') {
  //   const fileName = image.split('/').pop();
  //   return `${process.env.REACT_APP_IMAGES_PATH}/${fileName}`;
  // }

  return image;
}
